{
  "name": "ironhide-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "cors": "^2.8.5",
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^3.24.1",
    "nodemailer": "^9.0.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    
    "typescript": "^5.0.0"
  },
  "private": true
}
